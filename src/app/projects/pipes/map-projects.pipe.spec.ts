import { MapProjectsPipe } from './map-projects.pipe';

describe('MapProjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new MapProjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
